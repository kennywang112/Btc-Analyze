import { ElectrumApiInterface } from "../api/electrum-api.interface";
import { CommandInterface } from "./command.interface";
import { BaseRequestOptions } from "../interfaces/api.interface";
export declare class MintInteractiveDmitemWithRulesCommand implements CommandInterface {
    private electrumApi;
    private requestDmitem;
    private parentContainerAtomicalId;
    private address;
    private fundingWIF;
    private options;
    constructor(electrumApi: ElectrumApiInterface, requestDmitem: string, parentContainerAtomicalId: string, address: string, fundingWIF: string, options: BaseRequestOptions);
    run(): Promise<any>;
}
