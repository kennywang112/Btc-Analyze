import { ElectrumApiInterface } from "../api/electrum-api.interface";
import { CommandInterface } from "./command.interface";
import { BaseRequestOptions } from "../interfaces/api.interface";
export declare class MintInteractiveDitemWithRulesCommand implements CommandInterface {
    private electrumApi;
    private requestDmitem;
    private parentContainer;
    private parentContainerAtomicalId;
    private address;
    private fundingWIF;
    private options;
    constructor(electrumApi: ElectrumApiInterface, requestDmitem: string, parentContainer: string, parentContainerAtomicalId: string, address: string, fundingWIF: string, options: BaseRequestOptions);
    run(): Promise<any>;
}
